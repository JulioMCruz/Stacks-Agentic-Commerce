import { ContractCall, StacksTransaction } from '@stacks/transactions';

export function createAgentRegistryTransaction() {
  // Placeholder para transacciones del Agent Registry
  return {
    contractName: 'agent-registry',
    functionName: 'register-agent',
    args: [],
  };
}

export function createCommerceTransaction() {
  // Placeholder para transacciones del Agentic Commerce
  return {
    contractName: 'agentic-commerce',
    functionName: 'create-job',
    args: [],
  };
}
